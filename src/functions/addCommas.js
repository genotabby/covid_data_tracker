export default function addCommas(amount) {
  let a, b, c;
  //   if (amount > 1000000) {
  //     a = Math.trunc(amount / 1000000);
  //     b = Math.trunc((amount - a * 1000000) / 1000);
  //     c = Math.trunc(amount % 1000);
  //     return `${a},${b},${c}`;
  //   } else if (amount > 1000) {
  //     b = Math.trunc(amount / 1000);
  //     c = Math.trunc(amount % 1000);
  //     return `${b},${c}`;
  //   } else {
  return amount;
  //   }
}
